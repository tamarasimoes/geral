// exercicio 1
let palindromo = ['a', 'r', 'a', 'r', 'a'];

function verificaPalindromo (palindromo) {
    if (palindromo === palindromo.reverse()) {
        return true;
    }
    else {
        return false;
    }
}

console.log(verificaPalindromo(palindromo));

// exercicio 2
let teste = [2, 3, 6, 7, 10, 1];
function indexArray() {
    for (let index = 0; index <= teste; index++) {
        let maior = 0;
        let atual = teste[index];
        if (teste[index] > atual) {
            maior = array[i];
        }
    return maior;
    }
}

console.log(indexArray(teste));

//exercicio 7
let word = 'trybe';
let ending = 'be';
